#
# NixOS
#

# Check for Nix
if test ! $(which nix-shell)
then
    echo "Installing NixOS for you."

    echo "JUST KIDDING. This doesn't run in a script and DK is too lazy rn to fix it. Go to the ./nix/install.sh script
    and run the line yourself. Toodles."

    ########### RUN THIS MANUALLY, LOSER ########
    ## sh <(curl -L https://nixos.org/nix/install) --daemon
fi

exit 0

